import express from 'express';
// import { Imagem } from '../models/Imagem';

const router = express.Router();

// router.get('', async (_req, res) => {
//   res.send((await Imagem.findOne({ where: { id: 4 } })).conteudo);
// });

export default router;
